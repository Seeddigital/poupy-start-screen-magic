//import { createClient } from '@supabase/supabase-js';
//import type { Database } from './types';

const SUPABASE_URL = ''
const SUPABASE_PUBLISHABLE_KEY = ''

// Disabled supabase client - using mock auth instead
export const supabase = null as any;